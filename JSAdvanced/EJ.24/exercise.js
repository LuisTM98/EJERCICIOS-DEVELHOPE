const person = {
  id: 1,
  name: 'Mario',      //changed firstName to name
  surname: 'Rossi',   //changed lastName to surname
  old: 25             //changed age to old
};

const { id, name, surname, old } = person

console.log(id, name, surname, old);